# Legacy UI Preservation

The previous `app.py` is preserved as the UI reference from the user-supplied project package.

It is **not** part of the new data-plane execution path yet.

The rebuild will retain the existing Gradio interface and migrate its actions onto the new service/data boundaries only after the canonical full-prize data foundation is verified.
